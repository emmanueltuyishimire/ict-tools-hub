
import { faqData, howToSchema, keyTerminologies, softwareAppSchema } from '../data-transfer-calculator/schema';

// For the Cloud Sync Time Calculator, we can reuse the schema data from the Data Transfer Time Calculator
// as the concepts are identical. We re-export them here for clarity and proper module structure.

export { faqData, howToSchema, keyTerminologies, softwareAppSchema };
